algoritmo "Funcoes Aritmeticas"
var
   //A: Inteiro
   A: Real
inicio
      //A <- Abs(-50)  Valor Absoluto
      //A <- Exp(9,5)  Exponenciacao
      //A <- RaizQ(81) Raiz Quadrada
      //A <- Int(RaizQ(90))  Interio(Raiz Quadrada)
      //A<- GraupRad(90)   GraupRad
        A<- Sen(GraupRad(90))  Seno
      Escreva(A)
fimalgoritmo
